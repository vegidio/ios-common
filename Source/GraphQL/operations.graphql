mutation signIn($dto: SignInRequestDto!) {
    signIn(dto: $dto) {
        accessToken
        refreshToken
    }
}

query me {
    me {
        id,
        name,
        username,
        email
    }
}